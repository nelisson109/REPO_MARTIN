package com.martin.pruebasAnteriores;

public class Modificar extends VerPartidos {
 /*   public Modificar(){
        super();
        HBox hBox = new HBox();

        Button modificar = new Button("Modificar partido");
        modificar.setOnAction(new EventHandler<ActionEvent>() {
            @Override
            public void handle(ActionEvent actionEvent) {
                tableView.getSelectionModel().getSelectedIndex();
                //   tableView.getSelectionModel().select(tf.getRow(), tf.getTableColumn());
              //  Logica.getInstance().getPartidos().set(tableView.getSelectionModel().getSelectedIndex());
                // Logica.getInstance().borrarPartido(indiceBorrar);
            }
        });

        hBox.getChildren().add(modificar);
        Scene scene = new Scene(new VBox(tableView, hBox), 500, 500);
        this.setScene(scene);
        this.show();
    }*/
}
